kaborTBATS<-function(myvector,countDaysInYear,year,dayOfYear,forecastDuration){
	myts <-ts(myvector,  freq=countDaysInYear, start=c(year,dayOfYear ))
	mytstbats <- tbats(myts)
	mytimeforecast1 <-forecast(mytstbats, h=forecastDuration)
	myobj <- as.numeric(mytimeforecast1$mean)
	return (myobj)
}
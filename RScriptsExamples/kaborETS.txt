kaborETS<-function(myvector,countDaysInYear,year,dayOfYear,forecastDuration){
	myts <-ts(myvector,  freq=countDaysInYear, start=c(year,dayOfYear ))
	mytsets <- ets(myts)
	mytimeforecast1 <-forecast(mytsets, h=forecastDuration)
	myobj <- as.numeric(mytimeforecast1$mean)
	return (myobj)
}